# This image is intended for IPv4-only campus uplinks. Disable IPv6 at the
# kernel level so an automatic WAN6 route cannot bypass IPv4 policy routing.
net.ipv6.conf.all.disable_ipv6=1
net.ipv6.conf.default.disable_ipv6=1
